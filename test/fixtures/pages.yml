# == Schema Information
# Schema version: 20080728182013
#
# Table name: pages
#
#  id          :integer(11)     not null, primary key
#  uri_pattern :string(255)     
#  role_id     :integer(11)     
#  position    :integer(11)     
#  assertions  :string(255)     
#  variables   :string(255)     
#  section     :string(255)     
#

google_com:
  role: web_site_external
  uri_pattern: "/"
  assertions: "assert_equal"
  variables:
  section:
  position: 1
google_com_about:
  role: web_site_external
  uri_pattern: "/about.html" # this page does actually exist
  assertions: "assert_equal"
  variables:
  section:
  position: 2
google_com_privacy:
  role: web_site_external
  uri_pattern: "/intl/en/privacy.html" # this page does actually exist
  assertions: "assert_equal"
  variables:
  section:
  position: 3

web_page1:
  role: web_app1
  uri_pattern: "/web/app/%display_id%"
  assertions: "assert_equal"
  variables: "display_id"
  section: "admin"
  position: 1
web_page2:
  role: web_app1
  uri_pattern: "/web/app/%display_id%/edit"
  assertions: "assert_equal"
  variables: "display_id"
  section: "admin"
  position: 2
client_page1:
  role: client_app1
  uri_pattern: "/client/app/%display_id%"
  assertions: "assert_equal"
  variables: "display_id"
  section: "displays"
  position: 3

localhost_index:
  role: web_site_localhost
  uri_pattern: "/"
  assertions:
  variables:
  section:
  position: 1
