# == Schema Information
# Schema version: 20080819160004
#
# Table name: roles
#
#  id   :integer(11)     not null, primary key
#  role :string(255)     
#

webapp:
  role: "web application server"

db:
  role: "application db server"

siteadmin:
  role: "application admin app server"

reactor:
  role: "physical reactor"

virt-reactor:
  role: "virtual reactor"

website:
  role: "generic web site"

groundcontrol:
  role: "the GroundControl application"
